package org.ucg.resource;

import jakarta.ws.rs.Path;

@Path("subscriptions")
public class ManageSubscriptions { // todo: migracija za popularne subscription-e, npr spotify, netflix, etc.

    // todo: flyway migracija + config

    // view single subscription

    // view all subscriptions (sa filterima)

    // add new subscription, ili odabrati od postojecih ili dodati rucno novu

    // delete

    // put (change tier etc)

    // get statistics
}
